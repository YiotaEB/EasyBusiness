<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.easybusiness.eb_androidapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.AdminMainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.Import"
            android:label="Import"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.Gallery"
            android:label="Gallery"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".UI.UserMainActivity" />
        <activity android:name=".UI.InvoiceActivity" />
        <activity android:name=".UI.ViewCustomersActivity" />
        <activity android:name=".UI.ViewSuppliersActivity" />
        <activity android:name=".UI.AddCustomersActivity" />
        <activity android:name=".UI.AddSuppliersActivity" />
        <activity android:name=".UI.ViewEmployeesActivity" />
        <activity android:name=".UI.AddEmployeesActivity" />
        <activity android:name=".UI.ViewSalesActivity" />
        <activity android:name=".UI.ViewInventoryActivity" />
        <activity android:name=".UI.ViewProductionActivity" />
        <activity android:name=".UI.ViewRoutesActivity" />
        <activity android:name=".UI.ViewDeliveryRoutesActivity"></activity>
    </application>

</manifest>